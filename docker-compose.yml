version: '2'
services:
    site:
        image: nginx
        ports:
            - "80:80"
        volumes:
            - ./html/:/usr/share/nginx/html:ro
    ipfs:
        image: ipfs/go-ipfs
        ports:
            - "8080:8080"
