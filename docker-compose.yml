version: '2'
services:
    site:
        image: nginx
        ports:
            - "80:80"
        volumes:
            - ./nginx/nginx.conf:/etc/nginx/nginx.conf
            - ./site/static:/usr/share/nginx/html:ro
    ipfs:
        image: ipfs/go-ipfs
        ports:
            - "8080:8080"
